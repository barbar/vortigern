import { expect, renderComponent } from '../../helpers/TestHelper';
import { Example } from './Example';

describe(('Example Container'), () => { 

	let component;

	beforeEach(() => {
		component = renderComponent(Example);
	})


});